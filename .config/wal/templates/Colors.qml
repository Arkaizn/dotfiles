pragma Singleton
import QtQuick 2.0

QtObject {{
    property color background: "#{background.strip}"
    property color foreground: "#{foreground.strip}"

    property color color0: "#{color0.strip}"
    property color color1: "#{color1.strip}"
    property color color2: "#{color2.strip}"
    property color color3: "#{color3.strip}"
    property color color4: "#{color4.strip}"
    property color color5: "#{color5.strip}"
    property color color6: "#{color6.strip}"
    property color color7: "#{color7.strip}"
    property color color8: "#{color8.strip}"
    property color color9: "#{color9.strip}"
    property color color10: "#{color10.strip}"
    property color color11: "#{color11.strip}"
    property color color12: "#{color12.strip}"
    property color color13: "#{color13.strip}"
    property color color14: "#{color14.strip}"
    property color color15: "#{color15.strip}"
}}

